# KMB 巴士到站時間查詢

一個簡單的繁體中文單頁應用，讓你輸入九巴路線，查看沿線車站及預計到站時間（直接使用九巴開放數據 API）。

## 使用方式
1. 直接開啟 `index.html`。
2. 輸入路線號碼（例如：`1A`、`68X`），按下「搜尋」。
3. 切換「去程 / 回程」標籤查看不同方向的車站列表。
4. 點擊任一車站，即可載入該站的 ETA、預計分鐘及備註。

## 特色
- 卡片式、行動友善的介面。
- 以九巴開放數據 API 取得路線、車站與 ETA（無需 API Key）。
- 支援鍵盤 Enter 搜尋，並快取站點資料以減少重複查詢。
